package module303;

import java.util.Scanner;

public class Arrays {
    public static void main(String[] args) {
//        double[] priceLists; it creates an double array and initialize size automatically
        /*
        Declaration using a New Operator
         Syntax → datatype[] arrayName = new datatype[arraySize];

         Example:
		double[] myList = new double[10]; //allocates memory for myList.
		myList[0] //references the first element in the array.
		myList[9] //references the last element in the array.



        Integer[] priceList = new Integer[5];
        String[] clientNames = {"Nurseit", "Guled", "Marta", "Soloni"};

        Scanner sc = new Scanner(System.in);

        for(int i = 0; i < priceList.length; i++){
            System.out.println("Enter price for product");
            priceList[i] = sc.nextInt();
        }
        for(int n : priceList){
            System.out.println(n);
        }
        */



    }
}
